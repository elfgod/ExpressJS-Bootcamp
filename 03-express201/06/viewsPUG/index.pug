doctype html
html
  head
    title PUG
    script(src='https://code.jquery.com/jquery-3.3.1.slim.min.js')
    link(href='https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css', rel='stylesheet')
  body 
    h1 Rendered Pug file
    div
      p This is a message
      p Another message
    div.container
      div.row
        div.col-sm-3 #{msg}
        div.col-sm-3 Your message is... #{msg}
        div.col-sm-6 Your message is... #{msg2}
        div.col-sm-12 #{html}
      div.row
        ul.col-sm-12
          each country in countries
            li= country.name + '--' + country.capital
            if country.western
              li= country.name + ' is a western country'
            else
              li= country.name + ' is a non-western country'

    div Message of the day: 2+2 = #{2+2}